function Navbar() {
  return (
    <nav>
      <h2>IMO Diamond Store BD</h2>

      <div>
        <button>Login</button>
        <button>Register</button>
      </div>
    </nav>
  );
}

export default Navbar;
